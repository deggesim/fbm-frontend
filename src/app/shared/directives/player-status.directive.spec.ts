import { PlayerStatusDirective } from './player-status.directive';

describe('PlayerStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new PlayerStatusDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
